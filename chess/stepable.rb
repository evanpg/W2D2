
module Stepable

  def moves(move)
    # first = self.pos[0] + move[0]
    # second = self.pos[1] + move[1]
    # if Board.valid_pos?([first,second])
    #   self.pos[0] += move[0]
    #   self.pos[1] += move[1]
    # end
  end

  private

  def move_diffs 
  end

end